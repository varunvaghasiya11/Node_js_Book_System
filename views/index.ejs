<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Management System</title>
    <!-- Google Fonts: Inter -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

    <style>
        :root {
            --primary-gradient: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
            --secondary-bg: #f3f4f6;
            --card-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
            --card-hover-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--secondary-bg);
            color: #1f2937;
        }

        .navbar {
            background: var(--primary-gradient);
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        }

        .navbar-brand {
            font-weight: 700;
            letter-spacing: -0.5px;
            font-size: 1.5rem;
        }

        .card {
            border: none;
            border-radius: 1rem;
            box-shadow: var(--card-shadow);
            transition: all 0.3s ease;
            background: white;
            overflow: hidden;
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: var(--card-hover-shadow);
        }

        .card-header {
            background: white;
            border-bottom: 1px solid #f3f4f6;
            padding: 1.5rem;
        }

        .form-floating>label {
            color: #6b7280;
        }

        .form-control:focus {
            border-color: #8b5cf6;
            box-shadow: 0 0 0 0.25rem rgba(139, 92, 246, 0.25);
        }

        .btn-primary {
            background: var(--primary-gradient);
            border: none;
            font-weight: 500;
            padding: 0.75rem 1.5rem;
        }

        .btn-primary:hover {
            opacity: 0.95;
            transform: translateY(-1px);
        }

        .book-card-img {
            height: 160px;
            background: linear-gradient(135deg, #e0e7ff 0%, #fae8ff 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            color: #6366f1;
            font-size: 4rem;
        }

        .badge-price {
            background: #ecfdf5;
            color: #059669;
            font-weight: 600;
            padding: 0.5rem 1rem;
            border-radius: 9999px;
        }
    </style>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark mb-5 sticky-top">
        <div class="container">
            <a class="navbar-brand" href="#">
                <i class="bi bi-book-half me-2"></i>Book Manager
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
        </div>
    </nav>

    <div class="container pb-5">
        <div class="row g-4">
            <!-- Add/Edit Form Section -->
            <div class="col-lg-4">
                <div class="card h-100">
                    <div class="card-header">
                        <h5 class="mb-0 fw-bold text-dark d-flex align-items-center">
                            <i
                                class="bi bi-<%= (typeof editBook !== 'undefined' && editBook) ? 'pencil-square' : 'plus-lg' %> me-2 text-primary"></i>
                            <%= (typeof editBook !=='undefined' && editBook) ? 'Edit Book' : 'Add New Book' %>
                        </h5>
                    </div>
                    <div class="card-body p-4">
                        <form
                            action="<%= (typeof editBook !== 'undefined' && editBook) ? '/update/' + editBook.id : '/' %>"
                            method="post">
                            <div class="form-floating mb-3">
                                <input type="text" class="form-control" name="title" id="title" required
                                    placeholder="Book Title"
                                    value="<%= (typeof editBook !== 'undefined' && editBook) ? editBook.title : '' %>">
                                <label for="title">Book Title</label>
                            </div>

                            <div class="form-floating mb-3">
                                <input type="text" class="form-control" name="author" id="author" required
                                    placeholder="Author Name"
                                    value="<%= (typeof editBook !== 'undefined' && editBook) ? editBook.author : '' %>">
                                <label for="author">Author Name</label>
                            </div>

                            <div class="form-floating mb-3">
                                <input type="number" step="0.01" class="form-control" name="price" id="price" required
                                    placeholder="Price"
                                    value="<%= (typeof editBook !== 'undefined' && editBook) ? editBook.price : '' %>">
                                <label for="price">Price ($)</label>
                            </div>

                            <div class="form-floating mb-4">
                                <textarea class="form-control" name="description" id="description"
                                    placeholder="Description"
                                    style="height: 120px"><%= (typeof editBook !== 'undefined' && editBook) ? editBook.description : '' %></textarea>
                                <label for="description">Description</label>
                            </div>

                            <div class="d-grid">
                                <button type="submit" class="btn btn-primary shadow-sm">
                                    <%= (typeof editBook !=='undefined' && editBook) ? 'Update Book' : 'Create Book' %>
                                </button>
                                <% if (typeof editBook !=='undefined' && editBook) { %>
                                    <a href="/" class="btn btn-light mt-2 text-muted">Cancel</a>
                                    <% } %>
                            </div>
                        </form>
                    </div>
                </div>
            </div>

            <!-- Available Books Section -->
            <div class="col-lg-8">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h4 class="mb-0 fw-bold text-dark">Library Collection</h4>
                    <span class="badge bg-white text-dark shadow-sm border px-3 py-2 rounded-pill">
                        <%= (typeof books !=='undefined' ) ? books.length : 0 %> Books
                    </span>
                </div>

                <% if (typeof books !=='undefined' && books.length> 0) { %>
                    <div class="row row-cols-1 row-cols-md-2 g-4">
                        <% books.forEach(function(book) { %>
                            <div class="col">
                                <div class="card h-100">
                                    <div class="book-card-img position-relative">
                                        <i class="bi bi-book"></i>
                                        <div class="position-absolute top-0 end-0 p-3">
                                            <div class="dropdown">
                                                <button class="btn btn-light btn-sm rounded-circle shadow-sm"
                                                    type="button" data-bs-toggle="dropdown">
                                                    <i class="bi bi-three-dots-vertical"></i>
                                                </button>
                                                <ul class="dropdown-menu dropdown-menu-end border-0 shadow-lg">
                                                    <li><a class="dropdown-item py-2" href="/view/<%= book.id %>"><i
                                                                class="bi bi-eye me-2 text-primary"></i>View Details</a>
                                                    </li>
                                                    <li><a class="dropdown-item py-2" href="/edit/<%= book.id %>"><i
                                                                class="bi bi-pencil me-2 text-warning"></i>Edit</a></li>
                                                    <li>
                                                        <hr class="dropdown-divider">
                                                    </li>
                                                    <li><a class="dropdown-item py-2 text-danger"
                                                            href="/delete/<%= book.id %>"><i
                                                                class="bi bi-trash me-2"></i>Delete</a></li>
                                                </ul>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="card-body p-4 d-flex flex-column">
                                        <h5 class="card-title fw-bold text-dark mb-1 text-truncate">
                                            <%= book.title %>
                                        </h5>
                                        <p class="text-muted small mb-3">by <span class="fw-medium text-dark">
                                                <%= book.author %>
                                            </span></p>

                                        <p class="card-text text-secondary small flex-grow-1"
                                            style="display: -webkit-box; -webkit-line-clamp: 3; line-clamp: 3; -webkit-box-orient: vertical; overflow: hidden;">
                                            <%= book.description %>
                                        </p>

                                        <div
                                            class="d-flex justify-content-between align-items-center mt-3 pt-3 border-top">
                                            <span class="badge-price">$<%= book.price %></span>
                                            <a href="/view/<%= book.id %>"
                                                class="btn btn-sm btn-outline-primary rounded-pill px-3">Read More</a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <% }); %>
                    </div>
                    <% } else { %>
                        <div class="card border-0 shadow-sm text-center py-5">
                            <div class="card-body">
                                <div class="display-1 text-muted mb-3 opacity-25">
                                    <i class="bi bi-journal-x"></i>
                                </div>
                                <h5 class="text-dark fw-bold">Your library is empty</h5>
                                <p class="text-muted">Start by adding your first book using the form.</p>
                            </div>
                        </div>
                        <% } %>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
        crossorigin="anonymous"></script>
</body>

</html>