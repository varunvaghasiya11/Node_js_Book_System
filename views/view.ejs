<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Book Details</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

    <style>
        :root {
            --primary-gradient: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
            --secondary-bg: #f3f4f6;
            --card-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--secondary-bg);
            color: #1f2937;
        }

        .navbar {
            background: var(--primary-gradient);
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        }

        .navbar-brand {
            font-weight: 700;
            letter-spacing: -0.5px;
            font-size: 1.5rem;
        }

        .book-card {
            max-width: 800px;
            margin: 0 auto;
            border: none;
            border-radius: 1rem;
            box-shadow: var(--card-shadow);
            background: white;
            overflow: hidden;
        }

        .page-header {
            background: linear-gradient(to right, #e0e7ff, #fae8ff);
            min-height: 200px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #6366f1;
        }

        .price-tag {
            background: #ecfdf5;
            color: #059669;
            font-size: 1.25rem;
            font-weight: 700;
            padding: 0.5rem 1.25rem;
            border-radius: 9999px;
            display: inline-block;
        }

        .btn-back {
            color: #6b7280;
            font-weight: 500;
            padding: 0.75rem 1.5rem;
            border-radius: 0.5rem;
            transition: all 0.2s;
        }

        .btn-back:hover {
            background-color: #f3f4f6;
            color: #1f2937;
        }
    </style>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark mb-5">
        <div class="container">
            <a class="navbar-brand" href="/"><i class="bi bi-book-half me-2"></i>Book Manager</a>
        </div>
    </nav>

    <div class="container pb-5">
        <div class="card book-card">
            <div class="page-header">
                <i class="bi bi-book" style="font-size: 5rem; opacity: 0.5;"></i>
            </div>

            <div class="card-body p-5 position-relative">
                <div class="position-lg-absolute top-0 end-0 p-4 mt-n5 me-3 d-flex gap-2 justify-content-end">
                    <a href="/edit/<%= book._id %>"
                        class="btn btn-warning shadow-sm text-white fw-bold px-4 rounded-pill">
                        <i class="bi bi-pencil me-2"></i>Edit
                    </a>
                    <a href="/delete/<%= book._id %>" class="btn btn-danger shadow-sm px-4 rounded-pill">
                        <i class="bi bi-trash me-2"></i>Delete
                    </a>
                </div>

                <div class="mt-4 mt-lg-0">
                    <h6 class="text-uppercase text-primary fw-bold letter-spacing-wide mb-2">Book Details</h6>
                    <h1 class="display-5 fw-bold text-dark mb-2">
                        <%= book.title %>
                    </h1>
                    <h4 class="text-muted fw-light fst-italic mb-4">
                        by <span class="text-dark fw-medium">
                            <%= book.author %>
                        </span>
                    </h4>

                    <div class="mb-5">
                        <span class="price-tag shadow-sm">$<%= book.price %></span>
                    </div>

                    <div class="mb-5">
                        <h5 class="fw-bold text-dark mb-3">About this book</h5>
                        <p class="lead text-secondary" style="line-height: 1.8;">
                            <%= book.description %>
                        </p>
                    </div>

                    <div class="d-flex justify-content-between align-items-center border-top pt-4">
                        <a href="/" class="btn btn-back">
                            <i class="bi bi-arrow-left me-2"></i>Back to Library
                        </a>
                        <small class="text-muted">ID: <%= book._id %></small>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
        crossorigin="anonymous"></script>
</body>

</html>